const Button = ({ message }) => {
    return (
        <button className="p-1 rounded-md border-2 border-white bg-blue-800 hover:bg-blue-600 transition-all duration-300">{message}</button>
    )
}

export default Button
